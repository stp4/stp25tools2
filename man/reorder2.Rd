% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/factor.R
\name{reorder2}
\alias{reorder2}
\title{Reorder factor levels by frequency, auxiliary variable, or custom rules}
\usage{
reorder2(
  x,
  ...,
  decreasing = TRUE,
  last = NULL,
  threshold = NULL,
  threshold.na.strings = "Other",
  reverse = FALSE
)
}
\arguments{
\item{x}{Ein Faktor.}

\item{...}{Optional:
- Ein numeric Vektor (gleich lang wie x) → fct_reorder()
- Ein character/factor Vektor (gleich lang wie x) → fct_relevel(), fehlende Levels → "Other"
- Ein oder mehrere benannte Character-Vektoren → fct_collapse()}

\item{decreasing}{Logical. Bei TRUE (default) wird absteigend sortiert.}

\item{last}{Character-Vektor von Levelnamen, die ans Ende verschoben werden.}

\item{threshold}{Numeric oder integer.
- NULL: kein Lumping
- integer oder "integer-like": benutze fct_lump_min(min = threshold)
- proportion (0 < threshold < 1): benutze fct_lump_prop(prop = threshold)}

\item{threshold.na.strings}{Name der Sammelkategorie beim Lumping. Default "Other".}

\item{reverse}{Logical. TRUE → Levelreihenfolge umkehren.}
}
\value{
Faktor mit geänderter Levelreihenfolge
}
\description{
Wrapper um forcats::fct_infreq(), fct_reorder(), fct_lump_min(),
fct_lump_prop(), fct_relevel() und fct_collapse(), der automatisch den
passenden Weg wählt.
}
\examples{
x <- factor(rep(1:5, c(10,20,5,2,8)), labels = letters[1:5])

# Frequenzsortierung
reorder2(x)

# Mit Lumping per Count
reorder2(x, threshold = 10)

# Mit Lumping per Proportion
reorder2(x, threshold = 0.2)

# Numerisches Hilfsargument
set.seed(1)
reorder2(x, rnorm(length(x)))

# Character-Vektor als Reihenfolge
reorder2(x, c("b","a"))

# Collapse mit benannten Character-Vektoren
reorder2(x,
  small = c("d","e"),
  big   = c("a","b","c")
)
}
