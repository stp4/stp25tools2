% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-trans-apply.R
\name{Index}
\alias{Index}
\title{Skalen zu einem Index zusammenfassen}
\usage{
Index(
  ...,
  na.rm = TRUE,
  as_percent = TRUE,
  fun = mean,
  digits = 2,
  re_code = NULL,
  min_level = NA,
  max_level = NA
)
}
\arguments{
\item{...}{Numerische Vektoren oder Faktoren, die zu einem Index
zusammengefasst werden sollen.}

\item{na.rm}{Logisch. Sollen fehlende Werte bei der Berechnung
ignoriert werden? Standard ist TRUE.}

\item{as_percent}{Logisch. Soll der Index auf den Bereich 0–1
skaliert werden? Standard ist TRUE.}

\item{fun}{Aggregationsfunktion zur Bildung des Index
(z. B. mean, median oder sum). Standard ist mean mit as_percent.}

\item{digits}{Anzahl der Nachkommastellen für das Ergebnis.
Standard ist 2.}

\item{re_code}{Numerischer Index der Variablen, die rekodiert
werden sollen (z. B. für invertierte Items). Standard ist NULL.}

\item{min_level}{Minimaler Skalenwert. Wird automatisch bestimmt,
wenn nicht angegeben.}

\item{max_level}{Maximaler Skalenwert. Wird automatisch bestimmt,
wenn nicht angegeben.}
}
\value{
Ein numerischer Vektor mit dem berechneten Index.
}
\description{
Berechnet einen zusammengesetzten Index aus mehreren Variablen
(z. B. Items einer Skala). Optional können Variablen rekodiert,
fehlende Werte behandelt und das Ergebnis als Prozentwert skaliert werden.
}
\details{
Faktoren werden automatisch in numerische Werte umgewandelt.
Wenn `re_code` gesetzt ist, werden die entsprechenden Variablen
invertiert (Spiegelung an min/max).
}
\examples{
# Beispiel: Bildung eines Index aus mehreren Items
Anamnesebogen <- Anamnesebogen |>
  mutate(
    wechsel = Index(
      hitzewallungen.schwitzen,
      depressive.verstimmungen,
      reizbarkeit,
      angstlichkeit,
      herzbeschwerden,
      vergesslichkeit,
      haarausfall
    )
  )

Anamnesebogen$wechsel
}
