% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-trans-wide.R
\name{Wide}
\alias{Wide}
\title{Convert Long Data to Wide Format with Flexible Input Options}
\usage{
Wide(
  data,
  key,
  ...,
  values_fill = NULL,
  names_sep = "_",
  names_vary = "slowest"
)
}
\arguments{
\item{data}{A data frame to reshape}

\item{key}{Either a formula (e.g., month ~ student) or unquoted column name(s)}

\item{...}{Columns to use as values (unquoted names)}

\item{values_fill}{Value to fill in missing combinations (default: NA)}

\item{names_sep}{Separator for new column names (default: "_")}

\item{names_vary}{Controls column name order ("fastest" or "slowest")}
}
\value{
A tibble in wide format
}
\description{
A more user-friendly wrapper around `tidyr::pivot_wider()` that supports
both formula and variable name syntax for reshaping data.
}
\examples{
df <- data.frame(
  month = rep(month.abb[1:3], 2),
  student = rep(c("Amy", "Bob"), each = 3),
  A = c(9, 7, 6, 8, 6, 9),
  B = c(6, 7, 8, 5, 6, 7),
  C = c(1, 3, 6, 3, 4, 7)
)

# Using variable names
Wide(df, student, A, B, C)

# Using formula syntax
Wide(df, month ~ student, A, B, C)
Wide(df, ~ student, A, B)

}
