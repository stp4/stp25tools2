% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-trans-long.R
\name{Long}
\alias{Long}
\alias{Long.formula}
\alias{Long.data.frame}
\title{Reshape Wide Data to Long Format}
\usage{
Long(x, ...)

\method{Long}{formula}(x, data, key = "variable", value = "value", use.label = TRUE, ...)

\method{Long}{data.frame}(
  x,
  ...,
  by = NULL,
  key = "variable",
  value = "value",
  id.vars = all.vars(by),
  use.label = TRUE,
  .list = NULL
)
}
\arguments{
\item{x}{Either a data frame or a formula specifying the reshaping}

\item{...}{Additional arguments passed to methods}

\item{data}{A data frame (required when x is a formula)}

\item{key}{Name for the new key column (default: "variable")}

\item{value}{Name for the new value column (default: "value")}

\item{use.label}{Logical indicating whether to use variable label (default: TRUE)}

\item{by}{Formula specifying grouping variables (alternative to id.vars)}

\item{id.vars}{Columns to keep as identifier variables}

\item{.list}{Optional list of data frames to bind before reshaping}
}
\value{
A data frame in long format
}
\description{
An enhanced version of `tidyr::pivot_longer()` that supports both formula and
variable name syntax, with additional features for handling labels and column specifications.
}
\examples{
df <- data.frame(
  month = rep(month.abb[1:3], 2),
  student = rep(c("Amy", "Bob"), each = 3),
  A = c(9, 7, 6, 8, 6, 9),
  B = c(6, 7, 8, 5, 6, 7),
  C = c(1, 3, 6, 3, 4, 7)
)

# Using variable names
Long(df, A, B, by = ~month)

# Using column positions
Long(df, 3, 4, 5, by = ~month)

# Using formula syntax
Long(A + B ~ month, df, key = "student", value = "grade")


}
